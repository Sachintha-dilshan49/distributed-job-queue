spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5433/jobqueue_test}
spring.datasource.username=${TEST_DB_USER:jobqueue}
spring.datasource.password=${TEST_DB_PASSWORD:jobqueue}

jobqueue.lease-seconds=30
jobqueue.work-duration-ms=40000
jobqueue.backoff-base-seconds=5
jobqueue.reaper-interval-ms=5000